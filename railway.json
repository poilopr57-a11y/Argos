{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.vpn_api"
  },
  "deploy": {
    "healthcheckPath": "/vpn/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "always"
  }
}
